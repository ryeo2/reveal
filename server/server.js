/**
 * Created by ryeo on 5/23/14.
 */


/*
if (Meteor.isServer) {

    sendStream = function (message) {
        slideStream.emit('message', message);
        console.log('me: ' + message);
    };
}
*/
